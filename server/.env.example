# 环境变量配置示例
# Environment Variables Configuration Example

# Flask运行环境 | Flask Environment
FLASK_ENV=production

# 数据库文件路径 | Database File Path
DATABASE_PATH=/app/data/database.db

# 单个数据最大大小限制（字节）| Maximum data size per entry (bytes)
MAX_DATA_SIZE=1048576

# 管理员密码（生产环境请修改）| Admin password (change in production)
ADMIN_PASSWORD=secure123

# 数据最大版本数 | Maximum number of data versions
MAX_VERSIONS=10

# 日志级别 | Log Level
LOG_LEVEL=INFO

# 服务器端口 | Server Port
PORT=5000

# 数据库备份间隔（小时）| Database backup interval (hours)
BACKUP_INTERVAL=24

# 最大备份数量 | Maximum number of backups
MAX_BACKUPS=7

# 启用HTTPS | Enable HTTPS
ENABLE_HTTPS=false

# SSL证书路径 | SSL Certificate Path
SSL_CERT_PATH=/etc/nginx/ssl/cert.pem

# SSL私钥路径 | SSL Private Key Path
SSL_KEY_PATH=/etc/nginx/ssl/key.pem

# CORS允许的源 | CORS Allowed Origins
CORS_ORIGINS=*

# JWT密钥（用于身份验证）| JWT Secret Key (for authentication)
JWT_SECRET_KEY=your-secret-key-here

# 会话超时时间（分钟）| Session Timeout (minutes)
SESSION_TIMEOUT=60